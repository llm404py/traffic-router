{
  "name": "traffic-router",
  "version": "1.0.0",
  "description": "Cloudflare Worker Traffic Routing Engine",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
